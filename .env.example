# Copia questo file in ".env" e inserisci le NUOVE chiavi (dopo la revoca).
# .env NON va mai committato (è già in .gitignore).
GEMINI_API_KEY=
RESEND_API_KEY=
RESEND_FROM=onboarding@resend.dev
EMAIL_TO=
